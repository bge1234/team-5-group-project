div(id="maparea" style="width: 800px; height: 400px;")
if events
  script.
   function initMap() {
     var eventsy = (!{JSON.stringify(events)})
     var map = new google.maps.Map(document.getElementById('maparea'), {
       zoom: 11,
       center: {lat: 39.739236, lng: -104.990251}
     });
     for (var i=0; i <= eventsy.length; i++) {
     if (eventsy[i].venue) {
       var marker = new google.maps.Marker({
           position: {lat: eventsy[i].venue.lat, lng: eventsy[i].venue.lon},
           map: map
         })
       }
     }
   }
    //-  var collection = (!{JSON.stringify(freebies)});
    //-    collection.forEach(function(addy,index){
    //-    $.ajax({
    //-      url: 'https://maps.googleapis.com/maps/api/geocode/json?address='+addy,
    //-      method: 'GET'
    //-      }).done(function(response){
    //-       console.log(response)
    //-        var loc = response.results[0].geometry.location
    //-     
    //-        var marker = new google.maps.Marker({
    //-          position: {lat: loc.lat, lng: loc.lng},
    //-          map: map
    //-       });
    //-     });
    //-  });
else
  script.
   function initMap() {
     var map = new google.maps.Map(document.getElementById('maparea'), {
       zoom: 14,
       center: {lat: #{lat}, lng: #{lng}},
     });
     var marker = new google.maps.Marker({
         position: {lat: #{lat}, lng: #{lng}},
         map: map
       });
   }

script(src="https://code.jquery.com/jquery-2.2.0.min.js")          
script(async defer src="https://maps.googleapis.com/maps/api/js?callback=initMap")
br
